//import { generatePhotosArray } from './data.js';
import { createPictureElement } from './pictureThumbnailsRenderer.js';

const photos = createPictureElement();

// eslint-disable-next-line no-console
console.log(photos);
